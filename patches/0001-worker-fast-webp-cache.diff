*** a/worker/Dockerfile
--- b/worker/Dockerfile
@@
-FROM python:3.11-slim
+FROM python:3.11-slim
 
 ENV PYTHONDONTWRITEBYTECODE=1 \
     PYTHONUNBUFFERED=1
 
 WORKDIR /app
 
-# poppler-utils / pdftoppm may быть установлены ранее
-RUN apt-get update && apt-get install -y --no-install-recommends poppler-utils curl && rm -rf /var/lib/apt/lists/*
+# MuPDF (mutool), libwebp (cwebp), coreutils для sha256sum, tini для корректного SIGTERM
+RUN apt-get update && apt-get install -y --no-install-recommends \
+    mupdf-tools libwebp-dev webp imagemagick coreutils tini curl \
+  && rm -rf /var/lib/apt/lists/*
 
 COPY requirements.txt .
 RUN pip install --no-cache-dir -r requirements.txt
 
 COPY app ./app
@@
-CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
+ENTRYPOINT ["/usr/bin/tini","--"]
+CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
